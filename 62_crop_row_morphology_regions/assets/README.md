# Day62 assets

Day62 的 CRDLD 原图、掩码和包含原图的对照图只保存在本地数据目录：

```text
D:/DL_code/data/crop_row_perception/day62_morphology_regions/
```

由于 CRDLD 许可仍未明确，这些文件不纳入 Git。仓库仅保存可复现代码、测试和不包含原始数据的学习笔记。

第二版主要产物为 `day62_results_v2.json`、`proxy_region_metrics_v2.csv` 和 `comparison_contact_sheet_v2.jpg`。纯透视面积过滤的未通过结果以 `*_perspective_only_rejected.*` 保留。
